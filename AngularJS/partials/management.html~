<!DOCTYPE html>
<html lang="de" ng-app="picube">  //sagt angular, wie das module heißt ('picube')
<head>
  <meta charset="utf8" />
  <meta http-equiv="X-UA-COMPATIBLE" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>PiCube</title>
  <link rel="icon" href="../favicon/favicon.ico">

  <!-- Bootstrap -->
  <link rel="stylesheet" href="../css/bootstrap.min.css">
  <link rel="stylesheet" href="../css/style.css">
</head>
<body ng-init="files=[]">

  <!-- Navbar -->
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Navigation ein-/ausblenden</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li class="active"><a href="#">Start</a></li>
          <li><a href="#upload">Upload File</a></li>
          <li><a href="#management">File Management</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Welcome-Greetings -->
    <div class="container" id="startpage">
        <h1>Welcome to PiCube!</h1>
        <h2>Please Choose a File to play</h2>
    </div>

  <!-- Upload File -->
  <div class="container" id="upload">
    <div class="jumbotron">
      <h2>Upload a File</h2>
      <p>Select Datapath: </p>
    </div>
  </div>

  <!-- List, Play and Delete Files -->
  <div class="container" id="management">
    <div class="jumbotron">
      <h2>File Management</h2>
      <p>This are your Files:</p>

      <!-- AngularJS -->
      <br/>
      <div class="container">
        <form>
          <input type="text" ng-model="search">
          <p>Searching for: {{search}}</p>
        </form>

      </div>

      <div ng-controller="FileCtrl">
        <div ng-hide="files.length" class="alert alert-warning">Noch keine Files vorhanden!</div>
        <table ng-show="files.length" class="table">
          <tr ng-repeat="file in files | filter:search" class="files-item">
            <td>{{file.id}}</td>
            <td>{{file.name}}</td>
            <td>{{file.path}}</td>
            <td><a href class="btn btn-default btn-sm" ng-click="files.playFile(file);">Play</a></td>
            <td><a href class="btn btn-default btn-sm" ng-click="files.removeFile(file);">Delete</a> </td>
          </tr>
          <tr>
            <td>Anzahl Files: {{files.length}}</td>
            <td></td>
          </tr>
        </table>
      </div>

    </div>
  </div>



  <script src="../js/jquery-1.11.2.min.js"></script>
  <script src="../js/bootstrap.min.js"></script>

  <!-- AngularJS -->
  <script src="../js/angular.js"></script>
  <script src="../js/angular-animate.js"></script>
  <script src="app.js"></script>        //Hier wird die eigentliche App geladen
</body>
</html>